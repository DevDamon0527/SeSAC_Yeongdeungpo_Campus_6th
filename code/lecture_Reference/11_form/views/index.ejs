<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
  </head>
  <body>
    <!-- <h1>폼 실습을 해보자!</h1> -->
    <h1><%= title %></h1>

    <!-- #1 form (get/post) -->
    <!-- name 속성: 데이터가 서버로 전송될 때, 폼 요소 값을 파악하기 위함 -->
    <h2>GET 요청</h2>
    <form action="/getForm" name="login1" method="get">
      <input type="text" name="id" placeholder="ID" />
      <input type="password" name="pw" placeholder="PW" />
      <button type="submit">제출</button>
    </form>

    <h2>POST 요청</h2>
    <form action="/postForm" name="login2" method="post">
      <input type="text" name="id" placeholder="ID" />
      <input type="password" name="pw" placeholder="PW" />
      <button type="submit">제출</button>
    </form>

    <!-- #2 form validation -->
    <!-- <h2>GET 요청</h2>
    <form action="/getForm" name="login1" method="get">
      <input
        type="text"
        name="id"
        placeholder="ID는 최소 3글자, 필수값"
        minlength="3"
        required
      />
      <input
        type="password"
        name="pw"
        placeholder="PW는 최소 4글자, 필수값"
        minlength="4"
        required
      />
      <input type="email" name="email" placeholder="EMAIL" required />
      <input type="number" name="grade" placeholder="GRADE" />
      <button type="submit">제출</button>
    </form>

    <h2>POST 요청</h2>
    <form action="/postForm" name="login2" method="post">
      <input
        type="text"
        name="id"
        placeholder="ID"
        pattern="([a-zA-Z0-9가-힣]){4,}"
        title="대소문자, 한글, 숫자, 4글자 이상"
        required
      />
      <input
        type="password"
        name="pw"
        placeholder="PW"
        pattern="([a-z0-9]){8,12}"
        title="소문자, 숫자로 8자리 이상 12자리 이하"
        required
      />
      <button type="submit">제출</button>
    </form> -->
    <!-- 정규식으로 비밀번호 패턴 검사하기: 조건은 소문자, 숫자로 8자 이상, 12자 이하 -->
    <!-- 
        html input pattern 사용 예시 
        - https://neomania.tistory.com/3 
        - https://www.w3schools.com/tags/att_input_pattern.asp
    -->

    <script>
      // form 태그의 name 속성 값으로 폼 선택하기
      console.log(document.forms['login1']);
      console.log(document.forms['login2']);
    </script>
  </body>
</html>
